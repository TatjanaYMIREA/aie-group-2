# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры отчёта

- max_hist_columns: 6
- top_k_categories: 5
- min_missing_share: 10%

## Качество данных (эвристики)

- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком много пропусков: **False**
- Оценка качества: **0.49**

### Новые творческие эвристики

- new_высокий_процент_уникальных_значений (>80% уникальных значений): **True**
- new_выбросы (1.5*IQR, >5% значений): **True**
- new_смешанные_типы (колонки с разными типами значений): **True**
- new_несбалансированные_категориальные (>90% одного значения): **False**
- new_много_повторяющихся_строк (>10% повторов): **False**

## Колонки с пропусками выше 10%

Нет колонок с пропусками выше порога.

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Топ-5 значений по колонкам см. в папке `top_categories/`

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
