# S03 – eda_cli: мини-EDA для CSV

Небольшое CLI-приложение для автоматического анализа CSV-файлов.  
Используется в рамках Семинара 03 курса «Инженерия ИИ».

---

## 📦 Требования

- **Python 3.11+**
- Установленный менеджер зависимостей **uv**  
  Документация: https://docs.astral.sh/uv/

---

## 🚀 Инициализация проекта

Выполните в корневой папке проекта (S03):

```bash
uv sync
````

Команда:

* создаст виртуальное окружение **`.venv`**;
* установит зависимости из **`pyproject.toml`**;
* установит CLI-команду **`eda-cli`** в ваше окружение.

---

## 🔧 Запуск CLI

### 📄 Команда `overview`

Быстрый текстовый обзор датасета:

```bash
uv run eda-cli overview data/example.csv
```

Доступные параметры:

* `--sep` — разделитель столбцов (по умолчанию `,`)
* `--encoding` — кодировка файла (по умолчанию `utf-8`)

---

## 📊 Полный отчёт: `report`

Запуск базового отчёта:

```bash
uv run eda-cli report data/example.csv --out-dir reports
```

После выполнения в указанной директории появятся файлы:

* **`report.md`** — основной отчёт в Markdown
* **`summary.csv`** — статистика колонок
* **`missing.csv`** — таблица пропусков
* **`correlation.csv`** — корреляционная матрица
* **`top_categories/*.csv`** — top-k категорий
* **`hist_*.png`** — гистограммы числовых колонок
* **`histograms_outliers/*.png`** — гистограммы с выделением выбросов
* **`barplots_top_categories/*.png`** — barplot-графики категорий
* **`missing_matrix.png`** — визуализация пропусков
* **`correlation_heatmap.png`** — тепловая карта корреляций

---

## 🆕 Новые параметры команды `report`

В рамках задания было добавлено несколько новых CLI-опций.

### Полный список новых аргументов:

| Параметр                  | Описание                                                               |
| ------------------------- | ---------------------------------------------------------------------- |
| **`--title`**             | Заголовок отчёта в начале `report.md`                                  |
| **`--max-hist-columns`**  | Сколько числовых колонок включать в гистограммы                        |
| **`--top-k-categories`**  | Сколько top-значений отображать для категориальных признаков           |
| **`--min-missing-share`** | Порог доли пропусков: колонки выше порога выводятся в отдельный список |

---

## ▶️ Пример вызова `report` с новыми опциями

```bash
uv run eda-cli report data/example.csv `
    --out-dir reports `
    --title "Отчёт по датасету Example" `
    --max-hist-columns 8 `
    --top-k-categories 7 `
    --min-missing-share 0.2
```

Этот вызов:

* вставит заголовок `"Отчёт по датасету Example"` в начало **report.md**,
* построит до **8 гистограмм** по числовым колонкам,
* покажет **top-7** значений в категориальных столбцах,
* выделит колонки с **долей пропусков > 20%** в отдельном разделе отчёта.

---

## 🧪 Тестирование

Запуск тестов:

```bash
uv run pytest -q
```

---

## 📁 Структура генерируемого отчёта

Файлы отчёта включают:

* `report.md` — читабельный итоговый отчёт;
* CSV-таблицы характеристик;
* визуализации распределений, пропусков, выбросов, категорий, корреляций.

---

## ✔️ Завершение

Приложение `eda-cli` служит компактным и удобным инструментом для автоматизации первичного анализа данных (EDA) с расширяемыми визуализациями и эвристиками качества.