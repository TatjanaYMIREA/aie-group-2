# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий |
|----|---------------------------------------------------------------------------|------------------|----------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅                | [`project/README.md`](README.md), раздел «4. Как запустить проект» |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅                | [`src/happiness/predict.py`](src/happiness/predict.py), [`artifacts/happiness_model.pkl`](artifacts/happiness_model.pkl) |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅                | [`notebooks/exp01_eda_baseline.ipynb`](notebooks/exp01_eda_baseline.ipynb) |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅                | [`notebooks/exp02_model_tuning.ipynb`](notebooks/exp02_model_tuning.ipynb), [`report.md`](report.md) раздел 5.2 |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅                | [`src/happiness/`](src/happiness/) — разделение на `api.py`, `predict.py`, `schemas.py`, `cli.py` |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅                | [`project/README.md`](README.md), раздел «4. Как запустить проект» (запуск через `uv run uvicorn`) |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅                | [`configs/.env.example`](configs/.env.example), `.env` в `.gitignore` |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅                | [`src/happiness/api.py`](src/happiness/api.py): `logging` + middleware + `/health` endpoint |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | [`report.md`](report.md), раздел 5.3 «Выбор финальной модели» |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅                | [`README.md`](README.md) раздел 7, [`report.md`](report.md) раздел 9 |

---

## Подсчёт баллов и связь с оценкой

За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.  
Максимум – 10 баллов.

**Итого: 10 / 10 баллов**

Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:

- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).

Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.

---

## Дополнительные комментарии

| Пункт | Комментарий |
|-------|-------------|
| **Пункт 6** | Dockerfile не используется, но в README описан чёткий сценарий развёртывания через `uv sync` и `uv run uvicorn` |
| **Логи** | Логи выводятся одновременно в консоль и в файл (`logs/app_YYYYMMDD.log`) |
| **Тесты** | 4 теста в [`tests/test_api.py`](tests/test_api.py) покрывают `/health`, корректные/некорректные запросы к `/predict` |

--- 
